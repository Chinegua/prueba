describe("Player", function() {


  it("should be able to play a Song", function() {

    expect(1).toEqual(1);
  });

  it("should be able to play a Song", function() {

    expect(3).toEqual(1);
  });
});
